import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    // justifyContent: "center",
    alignItems: "center",
    marginTop: 200,
  },
  buttonsContainer: {
    flexDirection: "row",
    gap: 12,
    marginTop: 100,
  },
});
